## Changes and Fixes in Invoice Tab ##


== Pdf Bill Changes ==

Payment History is getting printed in the invoice print,
what ever i print as an invoice, should be same as the order bill pdf and only change should be there is an Invoice Number in the Invoice PDF with the OrderID

== Action Buttons ==
add a hide unhide button for action buttons (Default is hidden action buttons)

Remove print button, View button has print option, so no need separate print button

Remove Deleet button

== Undo Button Function ==

If I undo anything done in the invoice tab, it should be undone in the all other tabs as well (for example if i undo an invoice, it should be undone in the order tab as well)

== Reduce the text size, because the Order IDs or Invoice IDs show as rows, not the single line text, fix this



